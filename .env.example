# Public (Vite — exposed to browser)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_STRIPE_PUBLISHABLE_KEY=pk_live_or_test_...
VITE_SITE_URL=https://your-domain.com

# Server only (Vercel + .env.local for `npm run dev` — never prefix with VITE_)
# Required for resource downloads (signed URLs on private storage).
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
STRIPE_SECRET_KEY=sk_live_or_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
