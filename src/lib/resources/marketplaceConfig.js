/**
 * When true, every published resource is free to download (no Stripe checkout).
 * Set to false and run paid-download restore migration when re-enabling sales.
 */
export const ALL_DOWNLOADS_FREE = true;
